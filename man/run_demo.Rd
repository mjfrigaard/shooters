% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_demo.R
\name{run_demo}
\alias{run_demo}
\title{Launch a demo Shiny app}
\usage{
run_demo(which = c("nested_modules", "single_module", "no_modules", "minimal"))
}
\arguments{
\item{which}{Character scalar; which demo app to launch. One of
\code{"minimal"}, \code{"no_modules"}, \code{"single_module"}, or \code{"nested_modules"}
(default).}
}
\value{
Called for its side effect (launches a Shiny app). Returns
invisibly.
}
\description{
Launches one of the example Shiny applications bundled with \code{shooters} in
\verb{inst/apps/}. Each app demonstrates a different structural pattern for use
with \code{\link[=ns_tree]{ns_tree()}}:
}
\details{
\itemize{
\item \code{"minimal"} — bare \code{ui} / \code{server} objects passed directly to
\code{shinyApp()}, no wrapper functions and no modules.
\item \code{"no_modules"} — helper functions wrapping \code{ui} and \code{server}, no Shiny
modules.
\item \code{"single_module"} — one module pair (\code{NS} + \code{moduleServer}).
\item \code{"nested_modules"} — modules that call other modules, spread across
multiple files.
}
}
\examples{
\dontrun{
run_demo()                   # nested_modules (default)
run_demo("single_module")
run_demo("no_modules")
run_demo("minimal")
}

}
